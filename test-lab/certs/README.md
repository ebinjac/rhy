# Generated test certificates

Run `../scripts/generate-test-certs.sh` from this directory's parent, or copy the
running Compose service's certificate bundle with `../scripts/export-test-certs.sh`.
Everything under `generated/` is local test material and is intentionally ignored.
